# Metis Runtime Pack Placeholder

Place a prepared `metisvm.bundle` directory or a `metis-runtime-bundle-v2` payload here before building the installer when you want the Windows setup package to include the VM runtime pack.

This placeholder keeps `electron-builder` wiring stable without committing large VM assets to the repository.
