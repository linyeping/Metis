# Changelog

## Unreleased

- Reorganized the repository into `desktop/`, `backend/`, and `docs/`.
- Renamed the Python backend package distribution to `metis-backend`.
- Updated Electron backend launch to use `python -m backend`.
- Moved tool implementations from `backend/Tools` to `backend/tools`.
- Renamed bridge contracts from `hermes_bridge` to `backend/bridges`.
- Added root-level README, contribution, security, architecture, and development docs.
