"""Metis backend core package.

- `backend.core.engine`：主循环、NLU、模式门控、上下文装配。
- `backend.core.memory`：对话上下文、Prompt 加载、工作区状态。
- `backend.core.prompts`：模块化 system prompt 文本目录。

请使用 `from backend.core.engine import ...` / `from backend.core.memory import ...`。
"""
